#include <bits/stdc++.h>
using namespace std;

int n, q, p, opt;
int d[200010], diff[200100];
bool turn = true, work = true;

int main() {
    turn = true, work = true;
	//turn 左为false, 右为true
	//work 改为true，不改为false
    cin >> n >> q >> p;
    for(; q--; ){
        
    }
}